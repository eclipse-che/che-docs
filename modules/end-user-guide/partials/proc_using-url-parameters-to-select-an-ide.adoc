[id="using-url-parameters-to-select-an-ide_{context}"]
== Using URL parameters to select an IDE

Use URL parameters to select your preferred IDE without impacting the IDE selection of other project users.

.Procedure

* When launching a workspace using the factory API, pass in additional URL parameters to load the chosen IDE:
+
```
https://<che_fqdn>#<git_repository_url>?che-editor=_<editor_key>_
```

.Additional resources

include::example$snip_{project-context}-editor-key-values.adoc[]