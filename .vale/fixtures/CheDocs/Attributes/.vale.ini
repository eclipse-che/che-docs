StylesPath = ../../../styles

MinAlertLevel = suggestion

# Use the file extension testadoc, to avoid testing this files in the GitHub workflow.
[formats]
testadoc = adoc

[*.{adoc,testadoc}]
CheDocs.Attributes = YES
