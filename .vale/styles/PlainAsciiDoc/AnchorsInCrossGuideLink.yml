---
extends: existence
message: "Use at most one anchor in cross-guide link '%s'"
ignorecase: true
level: error
nonword: true
scope: raw
tokens:
  - 'link:{prod-[[:alpha:]]*-url}[[:graph:]]*#[[:graph:]]*\['
