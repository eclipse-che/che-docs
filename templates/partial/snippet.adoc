// A text snippet is a section of text that is stored in an AsciiDoc file.
// A text snippet is not a module.
// It cannot include structural elements of a module such as an anchor ID or an H1 heading.
